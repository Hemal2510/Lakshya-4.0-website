
import RevealingSoonCard from "@/components/RevealingCard";

export default function SponsorsPage() {
    return (
        <main className="flex flex-col items-center justify-center min-h-screen">
           <RevealingSoonCard/>
        </main>
    );
}
